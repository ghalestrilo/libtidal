# Media Tools
- shell:
  - ["if [ ! comand -v obs ]; then sudo apt-get install -y obs-studio; fi", Installing OBS]
  - ["if [ ! comand -v musescore3 ]; then sudo apt-get install -y musescore3; fi", Installing Musescore]
  - ["if [ ! comand -v cmus ]; then sudo apt-get install -y cmus; fi", Installing cmus]
  - ["if [ ! comand -v cava ]; then sudo apt-get install -y cava; fi" , Installing cava]

- shell:
  - ["if [ ! comand -v cadence ]; then sudo apt-get install -y cadence; fi", Installing Cadence]
  - ["if [ ! comand -v catia ]; then sudo apt-get install -y catia; fi", Installing Catia]
  - ["if [ ! comand -v claudia ]; then sudo apt-get install -y claudia; fi", Installing Claudia]
  - sudo apt install qjackctl pulseaudio-module-jack # Jack Sink for Pulseaudio

# Live Coding Set
- shell:
  - [echo Setting up Tidal]
  - [sudo apt-get install -y supercollider, Installing Supercollider]
  - [sudo apt-get install -y sc3-plugins, Installing SC3 Plugins]
  - [sudo apt-get install -y haskell-platform, Installing Haskell (ghc, ghci)]
  - [sudo apt-get install -y cabal-install, Installing Cabal]
    # - [cabal update, Updating Cabal]
    # - [cabal install tidal, Installing Tidal]