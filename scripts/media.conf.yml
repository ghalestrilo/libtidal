# Media Tools
- shell:
  - ["if ! command -v obs; then sudo apt-get install -y obs-studio; fi", Installing OBS]
  - ["if ! command -v musescore3; then sudo apt-get install -y musescore3; fi", Installing Musescore]
  - ["if ! command -v cmus; then sudo apt-get install -y cmus; fi", Installing cmus]
  - ["if ! command -v cava; then sudo apt-get install -y cava; fi" , Installing cava]

- shell:
  - ["if ! command -v cadence; then sudo apt-get install -y cadence; fi", Installing Cadence]
  - ["if ! command -v catia; then sudo apt-get install -y catia; fi", Installing Catia]
  - ["if ! command -v claudia; then sudo apt-get install -y claudia; fi", Installing Claudia]
  - sudo apt install -y qjackctl pulseaudio-module-jack # Jack Sink for Pulseaudio

# Live Coding Set
- shell:
  - [echo Setting up Tidal]
  - [sudo apt-get install -y supercollider, Installing Supercollider]
  - [sudo apt-get install -y sc3-plugins, Installing SC3 Plugins]
  - [sudo apt-get install -y haskell-platform, Installing Haskell (ghc, ghci)]
  - [sudo apt-get install -y cabal-install, Installing Cabal]
    # - [cabal update, Updating Cabal]
    # - [cabal install tidal, Installing Tidal]